import Header from '../components/Header';

function Origins(props) {
  return (
    <>
      <Header fullsize={false} title={props.title} background={props.background}></Header>
    </>
  ); 
}

export default Origins;
